{{--  
<div class="grid grid-cols-1 md:grid-cols-3 gap-4">
    <div class="p-4 bg-white shadow rounded">
        <h2>Total Payments</h2>
        <p>{{ $totalPayments }}</p>
    </div>

    <div class="p-4 bg-white shadow rounded">
        <h2>Active Subscriptions</h2>
        <p>{{ $activeSubscriptions }}</p>
    </div>

    <div class="p-4 bg-white shadow rounded">
        <h2>Expired Subscriptions</h2>
        <p>{{ $expiredSubscriptions }}</p>
    </div>
</div>

<div class="mt-4">
    <h2>Top Members</h2>
    <ul>
        @foreach($topMembers as $member)
            <li>User ID: {{ $member->user_id }} - Total: {{ $member->total }}</li>
        @endforeach
    </ul>
</div>

<div class="mt-4">
    <h2>Top Trainers</h2>
    <ul>
        @foreach($topTrainers as $trainer)
            <li>Trainer ID: {{ $trainer->trainer_id }} - Machines: {{ $trainer->machines_count }}</li>
        @endforeach
    </ul>
</div>  --}}
